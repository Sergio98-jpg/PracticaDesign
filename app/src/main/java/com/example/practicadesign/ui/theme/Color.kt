package com.example.practicadesign.ui.theme

import androidx.compose.ui.graphics.Color

// ==================== PALETA DE COLORES - TEMA CLARO ====================

// Colores primarios (azul/cyan)
val PrimaryLight = Color(0xFF0891B2) // Cyan-600 - Color principal de la app
val PrimaryVariantLight = Color(0xFF0E7490) // Cyan-700
val PrimaryContainerLight = Color(0xFFF0FDFA) // Cyan-50

// Colores secundarios
val SecondaryLight = Color(0xFF8B5CF6) // Violet-500
val SecondaryVariantLight = Color(0xFF7C3AED) // Violet-600

// Colores de acento
val AccentLight = Color(0xFFF59E0B) // Amber-500
val AccentVariantLight = Color(0xFFD97706) // Amber-600

// Colores de superficie y fondo
val BackgroundLight = Color(0xFFF8FAFC) // Slate-50 - Fondo principal
val SurfaceLight = Color(0xFFFFFFFF) // Blanco - Superficies de tarjetas
val SurfaceVariantLight = Color(0xFFF1F5F9) // Slate-100

// Colores de texto
val OnPrimaryLight = Color(0xFFFFFFFF) // Blanco sobre primario
val OnSecondaryLight = Color(0xFFFFFFFF) // Blanco sobre secundario
val OnBackgroundLight = Color(0xFF0F172A) // Slate-900 - Texto principal
val OnSurfaceLight = Color(0xFF0F172A) // Slate-900 - Texto en superficies
val OnSurfaceVariantLight = Color(0xFF64748B) // Slate-500 - Texto secundario

// Colores de estado
val ErrorLight = Color(0xFFEF4444) // Red-500
val ErrorContainerLight = Color(0xFFFEE2E2) // Red-100
val OnErrorLight = Color(0xFFFFFFFF)

val SuccessLight = Color(0xFF10B981) // Green-500
val WarningLight = Color(0xFFF59E0B) // Amber-500
val InfoLight = Color(0xFF0891B2) // Cyan-600

// Colores de borde y divisores
val OutlineLight = Color(0xFFE2E8F0) // Slate-200
val OutlineVariantLight = Color(0xFFCBD5E1) // Slate-300

// Colores de riesgo (para zonas de riesgo)
val RiskHighLight = Color(0xFFEF4444) // Red-500
val RiskHighContainerLight = Color(0xFFFFEBEE) // Red-50
val RiskMediumLight = Color(0xFFF59E0B) // Amber-500
val RiskMediumContainerLight = Color(0xFFFDF7E7) // Amber-50
val RiskLowLight = Color(0xFF10B981) // Green-500
val RiskLowContainerLight = Color(0xFFF0FDF4) // Green-50

// ==================== PALETA DE COLORES - TEMA OSCURO ====================

// Colores primarios (azul/cyan) - Más brillantes en oscuro
val PrimaryDark = Color(0xFF22D3EE) // Cyan-400
val PrimaryVariantDark = Color(0xFF06B6D4) // Cyan-500
val PrimaryContainerDark = Color(0xFF164E63) // Cyan-900

// Colores secundarios
val SecondaryDark = Color(0xFFA78BFA) // Violet-400
val SecondaryVariantDark = Color(0xFF8B5CF6) // Violet-500

// Colores de acento
val AccentDark = Color(0xFFFBBF24) // Amber-400
val AccentVariantDark = Color(0xFFF59E0B) // Amber-500

// Colores de superficie y fondo
val BackgroundDark = Color(0xFF0F172A) // Slate-900 - Fondo principal
val SurfaceDark = Color(0xFF1E293B) // Slate-800 - Superficies de tarjetas
val SurfaceVariantDark = Color(0xFF334155) // Slate-700

// Colores de texto
val OnPrimaryDark = Color(0xFF0F172A) // Slate-900 sobre primario
val OnSecondaryDark = Color(0xFF0F172A) // Slate-900 sobre secundario
val OnBackgroundDark = Color(0xFFF1F5F9) // Slate-100 - Texto principal
val OnSurfaceDark = Color(0xFFF1F5F9) // Slate-100 - Texto en superficies
val OnSurfaceVariantDark = Color(0xFFCBD5E1) // Slate-300 - Texto secundario

// Colores de estado
val ErrorDark = Color(0xFFF87171) // Red-400
val ErrorContainerDark = Color(0xFF7F1D1D) // Red-900
val OnErrorDark = Color(0xFFF1F5F9)

val SuccessDark = Color(0xFF34D399) // Green-400
val WarningDark = Color(0xFFFBBF24) // Amber-400
val InfoDark = Color(0xFF22D3EE) // Cyan-400

// Colores de borde y divisores
val OutlineDark = Color(0xFF475569) // Slate-600
val OutlineVariantDark = Color(0xFF64748B) // Slate-500

// Colores de riesgo (para zonas de riesgo)
val RiskHighDark = Color(0xFFF87171) // Red-400
val RiskHighContainerDark = Color(0xFF7F1D1D) // Red-900
val RiskMediumDark = Color(0xFFFBBF24) // Amber-400
val RiskMediumContainerDark = Color(0xFF78350F) // Amber-900
val RiskLowDark = Color(0xFF34D399) // Green-400
val RiskLowContainerDark = Color(0xFF064E3B) // Green-900

// ==================== COLORES LEGACY (para compatibilidad) ====================
// Estos se mantienen para no romper código existente, pero se recomienda usar los nuevos

val Purple80 = Color(0xFFD0BCFF)
val PurpleGrey80 = Color(0xFFCCC2DC)
val Pink80 = Color(0xFFEFB8C8)

val Purple40 = Color(0xFF6650a4)
val PurpleGrey40 = Color(0xFF625b71)
val Pink40 = Color(0xFF7D5260)
